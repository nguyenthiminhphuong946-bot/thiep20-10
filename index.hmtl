<!DOCTYPE html>
<html lang="vi">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Chúc mừng 20-10</title>
<style>
  * { margin: 0; padding: 0; box-sizing: border-box; }
  body {
    height: 100vh;
    background: linear-gradient(135deg, #ffe6ec, #ffb6c1);
    display: flex;
    justify-content: center;
    align-items: center;
    flex-direction: column;
    overflow: hidden;
    font-family: 'Poppins', sans-serif;
    text-align: center;
  }

  /* Tiêu đề và nội dung */
  h1 {
    font-size: 3.5rem;
    color: #d63384;
    position: relative;
    z-index: 2;
    text-shadow: 2px 2px 8px rgba(255,255,255,0.8);
  }
  p {
    font-size: 1.3rem;
    color: #333;
    z-index: 2;
    position: relative;
    margin-top: 15px;
  }

  /* 💖 Trái tim nền sau chữ */
  .heart-bg {
    position: absolute;
    top: 45%;
    left: 50%;
    transform: translate(-50%, -50%) scale(1);
    width: 300px;
    height: 260px;
    background: radial-gradient(circle at 30% 30%, #ff4d79, #ff1a66);
    clip-path: path("M 150 40 C 150 0, 0 0, 0 90 C 0 140, 150 260, 150 260 C 150 260, 300 140, 300 90 C 300 0, 150 0, 150 40 Z");
    opacity: 0.3;
    animation: pulse 2.8s infinite ease-in-out;
    z-index: 1;
  }

  @keyframes pulse {
    0%, 100% { transform: translate(-50%, -50%) scale(1); opacity: 0.25; }
    50% { transform: translate(-50%, -50%) scale(1.1); opacity: 0.5; }
  }

  /* 🌹 Hoa hai bên */
  .rose {
    position: absolute;
    width: 110px;
    height: 110px;
    animation: float 5s ease-in-out infinite;
  }
  .rose.left { left: 8%; bottom: 15%; animation-delay: 0.5s; }
  .rose.right { right: 8%; bottom: 15%; animation-delay: 1s; }

  @keyframes float {
    0%, 100% { transform: translateY(0) rotate(0deg); }
    50% { transform: translateY(-20px) rotate(4deg); }
  }

  /* 🎈 Bong bóng trái tim */
  .bubble {
    position: absolute;
    bottom: -10px;
    background: rgba(255,105,180,0.6);
    border-radius: 50%;
    animation: rise 6s linear infinite;
  }

  @keyframes rise {
    0% { transform: translateY(0) scale(1); opacity: 0.9; }
    100% { transform: translateY(-110vh) scale(1.3); opacity: 0; }
  }

  /* 🌐 QR */
  .qr {
    position: fixed;
    bottom: 15px;
    right: 15px;
    width: 80px;
    background: white;
    border-radius: 10px;
    padding: 5px;
    box-shadow: 0 0 10px rgba(0,0,0,0.2);
  }

  /* 📱 Responsive */
  @media (max-width: 600px) {
    h1 { font-size: 2.5rem; }
    p { font-size: 1.1rem; }
    .rose { width: 75px; height: 75px; }
    .heart-bg { width: 200px; height: 180px; }
    .qr { width: 60px; }
  }
</style>
</head>
<body>

<div class="heart-bg"></div>

<h1>💖 20 - 10 💖</h1>
<p>Chúc bạn luôn xinh đẹp và dễ thương 💐</p>

<img src="https://cdn.pixabay.com/photo/2017/01/31/20/26/rose-2024973_1280.png" class="rose left">
<img src="https://cdn.pixabay.com/photo/2017/01/31/20/26/rose-2024973_1280.png" class="rose right">

<!-- QR code: sau khi mày có link web thật thì đổi src -->
<img class="qr" src="https://api.qrserver.com/v1/create-qr-code/?size=150x150&data=https://example.com" alt="QR code">

<script>
  // 🎈 Tạo bong bóng bay
  function createBubble() {
    const bubble = document.createElement('div');
    bubble.classList.add('bubble');
    document.body.appendChild(bubble);

    const size = Math.random() * 20 + 15 + 'px';
    bubble.style.width = size;
    bubble.style.height = size;
    bubble.style.left = Math.random() * window.innerWidth + 'px';
    bubble.style.background = 'rgba(255,105,180,' + (Math.random() * 0.4 + 0.3) + ')';
    bubble.style.animationDuration = Math.random() * 3 + 4 + 's';

    setTimeout(() => bubble.remove(), 7000);
  }
  setInterval(createBubble, 350);
</script>

</body>
</html>
